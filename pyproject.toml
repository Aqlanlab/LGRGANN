[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "lgrgann_wbocimp"
version = "1.0.0"
description = "Cost-Sensitive Defect Disposition Prediction with Label Reconstruction"
readme = "README.md"
requires-python = ">=3.9"
